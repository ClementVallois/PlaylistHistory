# crates/playlist-sync-app/Cargo.toml
[package]
name = "playlist-sync-app"
version = "0.1.0"
edition = "2024"

[dependencies]
playlist-sync-domain = { path = "../playlist-sync-domain" }
tracing.workspace = true
uuid.workspace = true
chrono.workspace = true

[lints]
workspace = true
